#include <bits/stdc++.h>
using namespace std;
using ll = long long;
int main() 
{
  int t;
  cin >> t;
  while(t--){
    ll n, m;
    cin >> n >> m;
    ll cnt = 0;
    for(int i = 1; i <= m; i++){
      cnt += (n+i) / (1LL*i*i);
    }
    cout << cnt - 1 << endl;
  }
}
